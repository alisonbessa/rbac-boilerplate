{
  "name": "@rbac-boilerplate/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "tsconfig.base.json",
    "eslint",
    "prettier"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo no-tests"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
