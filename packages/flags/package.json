{
  "name": "@rbac-boilerplate/flags",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@rbac-boilerplate/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
