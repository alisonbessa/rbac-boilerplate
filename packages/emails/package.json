{
  "name": "@rbac-boilerplate/emails",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo no-tests"
  },
  "dependencies": {
    "@rbac-boilerplate/config": "workspace:*",
    "nodemailer": "^6.10.1",
    "resend": "^4.8.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.15",
    "typescript": "^5.5.4"
  }
}
